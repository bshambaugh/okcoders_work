<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <% posts.forEach( function(post) { %>
       <h3><%= post.title %></h3>
       <span><%= post.body %></div>
    <% }) %>
  </body>
</html>
